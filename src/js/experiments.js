// experiments.js — DOM experiments for Week 3
// Add at least 5 experiments below. Comment each one to explain what it does.

// Example experiment:
// Change the text of the main heading
const heading = document.querySelector('h1');
if (heading) {
  heading.textContent = 'Welcome to My DOM Experiments!';
}
